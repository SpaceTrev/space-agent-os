'''Space-Claw automations package.

Sub-modules:
  git_ops         — git subprocess helpers
  code_writer     — parse LLM code blocks and write files to disk
  whatsapp_notify — POST outbound messages to OpenClaw gateway
'''
